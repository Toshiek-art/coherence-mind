---
const { title, eyebrow, intro } = Astro.props;
---
<div class="container space-y-4 py-12">
  {eyebrow && <p class="text-xs uppercase tracking-[0.3em] text-accent dark:text-accentMuted">{eyebrow}</p>}
  <h1 class="text-4xl font-semibold text-slate-900 dark:text-white">{title}</h1>
  {intro && <p class="max-w-3xl text-lg text-slate-600 dark:text-slate-300">{intro}</p>}
</div>
