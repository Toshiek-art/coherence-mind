---
const enableBanner = false;
---
{enableBanner && (
  <div class="fixed inset-x-0 bottom-0 z-10 border-t border-white/10 bg-ink/95 p-4 text-sm">
    <div class="container flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
      <p>
        We use optional analytics to understand how the Reflexive Coherence Model resonates. Enable
        them?
      </p>
      <div class="flex gap-3">
        <button class="btn-secondary" type="button">Decline</button>
        <button class="btn-primary" type="button">Accept</button>
      </div>
    </div>
    <!-- Integrate consent manager + analytics script loading here if optional services are added. -->
  </div>
)}
